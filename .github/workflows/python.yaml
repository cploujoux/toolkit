name: Python SDK

on:
  push:
    tags:
      - "*"
    branches:
      - "main"

jobs:
  pypi-publish:
    name: Upload to PyPI
    runs-on: ubuntu-latest
    environment: release
    permissions:
      # For PyPI's trusted publishing.
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - name: "Install uv"
        uses: astral-sh/setup-uv@v4
      - name: Set version
        if: startsWith(github.ref, 'refs/tags')
        run: |
          echo "Setting version to ${{ github.ref_name }}"
          sed -i -E 's/^version = .*/version = "${{ github.ref_name }}"/' sdk-python/pyproject.toml
      - name: Set version
        if: startsWith(github.ref, 'refs/tags') != true
        run: |
          echo ${{ github.ref }}
          echo "Setting version to ${{ github.ref_name }}"
          VERSION=$(git describe --tags --abbrev=0)
          echo "Current version: $VERSION"
          sed -i -E "s/^version = .*/version = \"${VERSION}.pre-${{ github.run_number }}\"/" sdk-python/pyproject.toml
      - name: Publish to PyPi
        run: |
          uv build sdk-python
          uv publish -v --trusted-publishing automatic sdk-python/dist/*
